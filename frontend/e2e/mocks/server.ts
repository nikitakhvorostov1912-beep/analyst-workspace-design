/**
 * E2E mock server placeholder.
 * Мокирование осуществляется через Playwright route() в fixtures.ts#setupRoutes().
 * MSW не используется (решение Plan 3.3 — Karpathy Simplicity First).
 *
 * Этот файл сохранён для документальных целей.
 * Реальный мок: page.route() в beforeEach каждого spec-файла.
 */

export {};
