/**
 * Версия приложения — единая точка истины для UI.
 *
 * Используется в Header (StencilLockup), /about, /guide заголовках, диагностиках.
 * При выпуске новой версии — поменять ОДНО место. До 2026-05-21 версия была
 * захардкожена в 3 разных файлах и регулярно расходилась (drift v1.2.1 / 1.2.2 / 1.2.4).
 *
 * Связано с desktop/package.json — должно совпадать. Не автоматизировано,
 * чтобы не тащить runtime fs-чтение в браузер.
 */
export const APP_VERSION = "1.2.15";
