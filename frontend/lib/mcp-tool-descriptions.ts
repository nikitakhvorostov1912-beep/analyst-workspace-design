/**
 * Краткие описания MCP-инструментов 1С — для tooltip'ов и сводок UI.
 *
 * Источник истины — `docs/00b-mcp-capability-map.md`. Полные карточки с
 * примерами вопросов — `/guide` → раздел «Инструменты в базе 1С».
 *
 * Используется на странице /status (tooltip при наведении на chip
 * инструмента), и потенциально в любых других местах где нужно показать
 * «что это вообще такое».
 *
 * Для незнакомого инструмента возвращается общая формулировка — UI остаётся
 * рабочим, даже если MCP Toolkit добавил что-то новое.
 */
const DESCRIPTIONS: Record<string, string> = {
  execute_query:
    "Запрос на языке 1С к данным базы (только чтение). Подсчёты, выборки, агрегаты, фильтры.",
  execute_code:
    "Произвольный код BSL на сервере 1С. Опасные операции (запись/удаление) требуют подтверждения.",
  get_metadata:
    "Структура базы: справочники, документы, регистры, реквизиты, табличные части, формы.",
  get_event_log:
    "Журнал регистрации: ошибки, действия пользователей, проведение документов. С фильтрами.",
  get_object_by_link:
    "Получить документ или элемент справочника по навигационной ссылке (1cv8s://...).",
  get_link_of_object:
    "Обратно: ссылка по описанию (тип + ключевые реквизиты). Зацепиться за объект по номеру.",
  find_references_to_object:
    "Где используется реквизит, общий модуль, регистр. Группирует по типу использования.",
  get_access_rights:
    "Права роли или пользователя на объект: чтение, изменение, добавление, удаление, проведение.",
  get_bsl_syntax_help:
    "Справочник по встроенному языку и API платформы 1С: сигнатуры, параметры, описание.",
  submit_for_deanonymization:
    "При включённой маскировке — восстанавливает реальные значения локально для следующего шага.",
  get_screenshot:
    "Снимок открытой формы 1С — модель может «увидеть» интерфейс, чтобы помочь с навигацией.",
  restart_1c_session:
    "Перезапустить сессию 1С на стороне MCP Toolkit — служебная операция при зависании.",
  close_1c_session:
    "Закрыть сессию 1С — освобождает ресурсы. Следующий запрос откроет новую.",
  // === aux: bsl-context (справочник API платформы) ===
  search:
    "Поиск по справочнику BSL: найти функцию платформы по фрагменту имени.",
  info:
    "Подробная справка о функции/типе платформы 1С — сигнатура, описание, примеры.",
  getMember:
    "Получить описание конкретного метода или свойства типа платформы.",
  getMembers:
    "Список методов и свойств типа платформы 1С.",
  getConstructors:
    "Список конструкторов типа: какие параметры можно передать в `Новый`.",
};

const DEFAULT_DESCRIPTION =
  "Инструмент MCP — модель ИИ вызывает его, чтобы получить данные или выполнить действие в 1С.";

export function describeMcpTool(name: string): string {
  return DESCRIPTIONS[name] ?? DEFAULT_DESCRIPTION;
}
