# 04 · Находки → правки (пофайлово)

> Главный рабочий документ. 12 находок аудита, отсортированы по тяжести.
> Для каждой: затронутые файлы, что сделать, ссылка на спеку. Реализовывать в
> порядке спринтов из `05`, не подряд.

Severity: 🔴 Critical · 🟠 High · 🔵 Medium

---

## 🔴 F-01 · Вы/ты вперемешку
**Файлы:** все с видимым текстом, в первую очередь `ComposerHub.tsx`, `Thread.tsx`,
`Input.tsx`, `settings/page.tsx`, все `publishToast(...)`, `OnboardingDialog.tsx`,
`lib/welcome-templates.ts`, `lib/slash-commands.ts`.
**Что делать:** применить таблицу замен из `02-ГОЛОС-И-ТОН.md` (2.3). Регистр — «вы»
строчное. Прогнать grep по `'Напиши'|'Задай'|'Проверь'|'спросим'|'Введи '` и вычистить.
**Готово когда:** ни одного «ты»-глагола и «backend/канал» в видимом UI.

---

## 🔴 F-02 · Невозможно проверить, откуда цифра
**Файлы:** `chat/AnswerSources.tsx` → новый `chat/AnswerProvenance.tsx`,
`chat/AssistantMessage.tsx`, `chat/TraceSummary.tsx`, `cards/TableCard.tsx`.
**Что делать:** реализовать «Грунт ответа» по `03-КОМПОНЕНТЫ.md` (3.3):
- Полоса «Грунт» под текстом ответа (всегда видна при наличии tool_calls).
- Клик «показать» раскрывает сам запрос к 1С (из `TraceSummary`/`tool_calls.args`) на
  `--code-bg` + длительность + N строк.
- В `TableCard` усечение (`truncated`/`total_available`) промотать в шапку карточки
  (см. 3.7), чтобы решение по неполным данным было заметно.
**Готово когда:** под любым ответом с запросом видно «Грунт», и сам запрос
открывается в один клик без разворачивания полного трейса.

---

## 🔴 F-03 · Шапка — ящик с 10 иконками
**Файлы:** `shell/Header.tsx`, `shell/ChannelSelector.tsx`, `shell/ModelBadge.tsx`,
`shell/AnonymizationStatus.tsx`, `knowledge/KnowledgeBadge.tsx`.
**Что делать:** по `03-КОМПОНЕНТЫ.md` (3.6) — разделить «статус» и «действия»,
справку (`Activity`/`BookOpen`/`HelpCircle` → `/status`,`/guide`,`/about`) собрать
в одно меню `?` с подписями (`dropdown-menu`). Цели ≥ 32px.
**Готово когда:** в шапке максимум 4 действия-иконки; помощь — одно меню с текстом;
статус базы/модели читается как единый блок.

---

## 🔴 F-04 · Пять систем ошибок + жаргон
**Файлы:** новый `ui/Alert.tsx`; переписать `ui/ErrorBanner.tsx`,
`shell/BackendDownBanner.tsx`, `chat/ConnectionStatusBanner.tsx`; правки в
`chat/AssistantMessage.tsx` (убрать `⚠`), `settings/page.tsx` (`border-red-800`).
**Что делать:** создать `Alert` (3.1), все ошибки — через него. Тексты — из 02.3.
Убрать emoji `⚠`. Иконки — lucide.
**Готово когда:** один визуальный язык ошибок во всём приложении, без emoji и без
слов «backend/SSE/MCP/env» в тексте для пользователя.

---

## 🟠 F-05 · Перегруз стенсил-текстуры
**Файлы:** глобально; `design-tokens.css` (типографика 01.7), `Sidebar.tsx`,
`ComposerHub.tsx`, `ui/SectionHead.tsx`, `ui/Marker.tsx`, любые eyebrow’ы.
**Что делать:**
- Оранжевый только на действие/фокус/«живое» — убрать декоративные оранжевые маркеры
  там, где они не действие.
- Eyebrow — один на секцию, трекинг .1em, размер ≥ 11px.
- Mono — лого/дисплей/данные, не на каждой подписи.
**Готово когда:** на ключевых экранах глаз сразу находит главное действие; нет
«ковра» из uppercase-микролейблов.

---

## 🟠 F-06 · У ответа нет лица и действий
**Файлы:** `chat/AssistantMessage.tsx`, `chat/Message.tsx`, `ui/CopyButton.tsx`.
**Что делать:** по 3.4 — аватар-глиф + имя + время + длительность; hover-действия
(копировать/повторить/источник); тело 15px. Реплика пользователя — выровнять стиль.
**Готово когда:** ответ имеет визуальный якорь, его можно скопировать и повторить
с клавиатуры и мышью; видно время.

---

## 🟠 F-07 · Онбординг — стена настроек
**Файлы:** `onboarding/OnboardingDialog.tsx`, `onboarding/StepIndicator.tsx`,
`onboarding/OnboardingResumeBanner.tsx`, `app/page.tsx` (вызов онбординга).
**Что делать:** перестроить поток:
1. **Экран ценности** (без техники): «Задавайте вопросы своей базе 1С обычными
   словами» + 3 примера того, что можно спросить. Кнопка «Начать».
2. **Развилка ролей:** «Я аналитик — настрою сам» / «Мне всё настроил ИТ-отдел».
   - «настрою сам» → текущие шаги «База 1С» + «Модель» (формы оставить, тексты — 02).
     Жаргон про порт/обработку показывать только здесь.
   - «настроил ИТ» → пропустить настройку, сразу в работу (если бэкенд уже сконфигурён).
3. **Память** — НЕ отдельный шаг, а тумблер на финальном экране «Готово».
4. Финал — три источника знаний + quick-start вопросы (уже есть, сохранить).
- «Пропустить» не должен вести в сломанное пустое состояние: если базы/модели нет,
  финал ведёт в Настройки, а не в чат.
**Готово когда:** новичок видит ценность до запроса техданных; бизнес-пользователь
может пройти онбординг, не встретив слова «порт/обработка/ключ» (если ИТ настроил).

---

## 🟠 F-08 · Карточка — стопка полос и мелких кнопок
**Файлы:** `cards/CardHeader.tsx`, `cards/TableCard.tsx`, `ui/table.tsx`,
`chat/ToolTrace.tsx` (категориальная радуга).
**Что делать:** по 3.7 — один заголовок, усечение в чип, зебра-таблица, tabular-nums.
По 01.6 — свести цвета чипов трейса к ok/error (убрать blue/green/purple).
**Готово когда:** вокруг таблицы максимум одна полоса хрома; данные читаются; трейс
не «радужный».

---

## 🔵 F-09 · Счётчик токенов и шум композера
**Файлы:** `chat/Input.tsx`, `chat/ComposerHub.tsx`.
**Что делать:** по 3.5 — удалить «N / 4000 ТОКЕНОВ», заменить тонкой полосой; один
hint `/ — команды · @ — объекты базы`; убрать дублирующийся brand-tick.
**Готово когда:** под полем ввода нет технических чисел; есть подсказка про «/» и «@».

---

## 🔵 F-10 · Один объект — четыре названия
**Файлы:** глобально (UI-строки), сверяться с глоссарием 02.2.
**Что делать:** привести видимый текст к словам База/Чат/Память/Модель. «Канал»,
«сессия», «обучение», «LLM» — только во внутреннем коде/типах, не в UI.
**Готово когда:** grep по UI-строкам не находит «канал/сессия/обучение/LLM» в видимом тексте.

---

## 🔵 F-11 · Сайдбар без поиска/закрепления/переименования
**Файлы:** `shell/Sidebar.tsx`, `shell/SessionList.tsx`.
**Что делать:** по 3.8 — поиск сверху, hover-меню (Закрепить/Переименовать/Удалить),
свёрнутый режим = иконки чатов, тумблер сворачивания вверх.
**Готово когда:** чат можно найти поиском, закрепить и переименовать; свёрнутый
сайдбар не пустой.

---

## 🔵 F-12 · Два разных пустых экрана
**Файлы:** `chat/Thread.tsx` (EmptyState), `chat/ComposerHub.tsx`,
`lib/welcome-templates.ts`, `ui/EmptyState.tsx`.
**Что делать:** по 3.2 — единый компонент пустого состояния и один источник примеров
(экспорт `EXAMPLE_PROMPTS`), используемый и на главной, и в пустом чате. Копирайт — 02.
**Готово когда:** оба пустых места используют один компонент и один набор примеров.

---

## Прочее (попутно, не отдельные находки)
- **Удалить мёртвые accent-темы** (`blue/clinical/indigo/orange`) из `design-tokens.css` (01.5).
- **Скроллбар** в `globals.css` использует хардкод `#262626`/`#303030` — заменить на
  токены (`--bd-2`/`--bd-3`), чтобы он подстраивался под light-тему.
- **Радиусы** привести к шкале 6/10/14/999 (01.8).
