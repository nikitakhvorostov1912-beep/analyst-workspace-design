# Plan 8.1: Project Skills + Rules Scaffolding

**Goal:** Создать 3 проектных скилла + 3 файла правил в `.claude/skills/` и `.claude/rules/`.

**Mode:** mvp
**Owner:** Claude Code
**Effort:** S (1-2 часа)

## Tasks

### T-08-01-1: Создать структуру папок (5 мин)

```powershell
$base = 'C:\CLOUDE_PR\projects\analyst-workspace-design\.claude'
@(
    'skills/awd-dev-up/scripts',
    'skills/awd-quality-gate/scripts',
    'skills/awd-claude-design-handoff/scripts'
) | ForEach-Object {
    New-Item -ItemType Directory -Path (Join-Path $base $_) -Force | Out-Null
}
```

**Verify:** `Get-ChildItem .claude/skills` показывает 3 папки.

### T-08-01-2: awd-dev-up skill (20 мин)

NEW `.claude/skills/awd-dev-up/SKILL.md`:
```markdown
---
name: awd-dev-up
description: Поднять backend (:8010) + frontend (:3010) для analyst-workspace-design с автоматическим verify
---

# /awd-dev-up

Запускает оба сервера проекта одной командой с health check.

## Когда вызывать
- «подними сервера», «запусти dev», «start», «открой приложение»
- В начале сессии когда нужно поработать с UI

## Что делает
1. Останавливает процессы на :8010 и :3010 (если есть)
2. Стартует backend uvicorn в `backend/.venv`
3. Стартует `npx next dev -p 3010` в `frontend/`
4. Polling до Ready (timeout 60 сек): `/health` 200 + HTML с правильным title
5. Output: URLs обоих сервисов

## Запуск
`pwsh .claude/skills/awd-dev-up/scripts/up.ps1`

## Caveat
- Требует `pnpm` или `npx` в PATH
- Требует `.venv` в `backend/`
```

NEW `.claude/skills/awd-dev-up/scripts/up.ps1`:
```powershell
$ErrorActionPreference = 'Stop'

# 1. Stop existing on :8010 / :3010
Get-NetTCPConnection -LocalPort 8010, 3010 -ErrorAction SilentlyContinue | ForEach-Object {
    Stop-Process -Id $_.OwningProcess -Force -ErrorAction SilentlyContinue
}
Start-Sleep -Seconds 1

# 2. Resolve dirs
$root = Resolve-Path (Join-Path $PSScriptRoot '..\..\..\..')
$backendDir = Join-Path $root 'backend'
$frontendDir = Join-Path $root 'frontend'
$venvPython = Join-Path $backendDir '.venv\Scripts\python.exe'

if (-not (Test-Path $venvPython)) { Write-Error "FAIL: $venvPython not found"; exit 1 }

# 3. Start backend (background)
Start-Process -FilePath $venvPython `
    -ArgumentList '-m', 'uvicorn', 'app.main:app', '--reload', '--port', '8010' `
    -WorkingDirectory $backendDir `
    -WindowStyle Hidden -PassThru | Out-Null

# 4. Start frontend (background)
$npxCmd = (Get-Command npx.cmd -ErrorAction SilentlyContinue)?.Source
if (-not $npxCmd) { $npxCmd = (Get-Command npx).Source }
Start-Process -FilePath $npxCmd `
    -ArgumentList 'next', 'dev', '-p', '3010' `
    -WorkingDirectory $frontendDir `
    -WindowStyle Hidden -PassThru | Out-Null

# 5. Poll
$ok = $false
for ($i = 0; $i -lt 30; $i++) {
    Start-Sleep -Seconds 2
    try {
        $h = (Invoke-WebRequest http://127.0.0.1:8010/health -UseBasicParsing -TimeoutSec 2).Content
        $f = (Invoke-WebRequest http://127.0.0.1:3010 -UseBasicParsing -TimeoutSec 2).Content
        if ($h -match '"status"\s*:\s*"ok"' -and $f -match '<title>1С Аналитик</title>') {
            $ok = $true
            break
        }
    } catch {}
}

if ($ok) {
    Write-Output "PASS Backend: http://127.0.0.1:8010"
    Write-Output "PASS Frontend: http://localhost:3010"
    exit 0
} else {
    Write-Error "FAIL: серверы не отвечают за 60 сек"
    exit 1
}
```

### T-08-01-3: awd-quality-gate skill (15 мин)

NEW `.claude/skills/awd-quality-gate/SKILL.md`:
```markdown
---
name: awd-quality-gate
description: Прогнать pytest + vitest + pnpm build + playwright и вернуть PASS/FAIL по каждому
---

# /awd-quality-gate

Полный qualitative прогон перед коммитом / релизом.

## Когда вызывать
- «проверь качество», «quality gate», «перед коммитом», «всё ли зелёное»

## Что делает (НЕ fail-fast)
1. `pytest backend/tests` (timeout 120s)
2. `pnpm vitest run` в `frontend/`
3. `pnpm build` в `frontend/`
4. `pnpm playwright test --reporter=line` в `frontend/`

Все 4 шага выполняются независимо. Output — 4 строки + общий вердикт.

## Запуск
`pwsh .claude/skills/awd-quality-gate/scripts/gate.ps1`

## Exit codes
- 0: все 4 PASS
- 1: хотя бы один FAIL
```

NEW `.claude/skills/awd-quality-gate/scripts/gate.ps1`:
```powershell
$root = Resolve-Path (Join-Path $PSScriptRoot '..\..\..\..')
$results = [ordered]@{}

Push-Location (Join-Path $root 'backend')
& .\.venv\Scripts\pytest tests --tb=short -q 2>&1 | Out-Null
$results['pytest'] = $LASTEXITCODE -eq 0
Pop-Location

Push-Location (Join-Path $root 'frontend')
pnpm vitest run 2>&1 | Out-Null
$results['vitest'] = $LASTEXITCODE -eq 0

pnpm build 2>&1 | Out-Null
$results['build'] = $LASTEXITCODE -eq 0

pnpm playwright test --reporter=line 2>&1 | Out-Null
$results['playwright'] = $LASTEXITCODE -eq 0
Pop-Location

Write-Output ""
foreach ($k in $results.Keys) {
    $status = if ($results[$k]) { 'PASS' } else { 'FAIL' }
    Write-Output "[$status] $k"
}
$allOk = -not ($results.Values | Where-Object { -not $_ })
Write-Output ""
if ($allOk) { Write-Output "OVERALL: PASS"; exit 0 } else { Write-Output "OVERALL: FAIL"; exit 1 }
```

### T-08-01-4: awd-claude-design-handoff skill (20 мин)

NEW `.claude/skills/awd-claude-design-handoff/SKILL.md`:
```markdown
---
name: awd-claude-design-handoff
description: Собрать context bundle для claude.ai/design — скриншоты + ban-list + промпт-шаблон
---

# /awd-claude-design-handoff

Готовит handoff bundle для итерации UI через claude.ai/design в браузере пользователя.

## Когда вызывать
- «хочу итерировать дизайн через claude design»
- «обнови UI», «новый дизайн страницы X»
- Пользователь сам говорит про claude.ai/design

## Что делает
1. Поднимает серверы (если не запущены) через awd-dev-up
2. Создаёт `temp/design-bundle-<timestamp>/`
3. Копирует: `CLAUDE.md`, `PROJECT.md`, `REQUIREMENTS.md`, `.claude/memory/design-constraints.md`
4. Снимает 3 screenshot через Playwright headed: `/` (Onboarding или Thread), `/settings`, `/sessions/{any}`
5. Генерирует `PROMPT.md` — шаблон для claude.ai/design с ban-list verbatim

## После генерации
Пользователь:
1. Открывает `claude.ai/design` в своём браузере
2. Drag-and-drop папку `temp/design-bundle-<ts>/`
3. Копирует промпт из `PROMPT.md` в чат
4. Итерирует
5. Export ZIP → импорт через `/from-design <path>`
```

NEW `.claude/skills/awd-claude-design-handoff/scripts/bundle.ps1`:
```powershell
$root = Resolve-Path (Join-Path $PSScriptRoot '..\..\..\..')
$ts = Get-Date -Format 'yyyyMMdd-HHmmss'
$bundleDir = Join-Path $root "temp\design-bundle-$ts"
New-Item -ItemType Directory -Path $bundleDir -Force | Out-Null
New-Item -ItemType Directory -Path (Join-Path $bundleDir 'screenshots') -Force | Out-Null

# 1. Copy context files
Copy-Item (Join-Path $root 'CLAUDE.md') $bundleDir
Copy-Item (Join-Path $root 'PROJECT.md') $bundleDir
Copy-Item (Join-Path $root 'REQUIREMENTS.md') $bundleDir
Copy-Item (Join-Path $root '.claude\memory\design-constraints.md') $bundleDir

# 2. Verify servers up (call awd-dev-up if not)
try { $null = Invoke-WebRequest http://127.0.0.1:3010 -UseBasicParsing -TimeoutSec 2 }
catch { & (Join-Path $PSScriptRoot '..\..\awd-dev-up\scripts\up.ps1') }

# 3. Playwright screenshots (через npx)
$pwScript = Join-Path $bundleDir 'screenshot.mjs'
@'
import { chromium } from "playwright";
const browser = await chromium.launch({ headless: true });
const page = await browser.newPage({ viewport: { width: 1440, height: 900 } });
await page.goto("http://localhost:3010");
await page.screenshot({ path: "screenshots/01-main.png", fullPage: true });
await page.goto("http://localhost:3010/settings");
await page.screenshot({ path: "screenshots/02-settings.png", fullPage: true });
await browser.close();
'@ | Out-File -FilePath $pwScript -Encoding UTF8
Push-Location $bundleDir
npx playwright install chromium 2>&1 | Out-Null
node screenshot.mjs
Pop-Location

# 4. PROMPT.md
@'
# Промпт для Claude Design — 1С Аналитик

Итерируй UI этого приложения. Сохрани концепцию **«чат во главе»** — единственный workflow: NL → LLM → tool_calls → cards.

## Стек (locked)
Next.js 15 + React 19 + shadcn/ui + Tailwind 4 + IBM Plex Sans/Mono. Тёмная тема by default. Русский UI. Desktop ≥ 1280px.

## Палитра
- Фон: #0a0a0a
- Текст: #e5e5e5
- Accent: clinical-blue #5cb8ff
- Borders: #1f1f1f

## Что НЕ делать (verbatim из design-constraints.md)
- ❌ «Work-modes» Discovery/Triage/Investigate/Mapping/Knowledge
- ❌ Object-centric IDE с tree метаданных слева
- ❌ Workflow editor с карточками операций
- ❌ AI right-rail с «инсайтами»
- ❌ Inter font, purple-cyan gradients, glass morphism, decorative emoji
- ❌ Mobile-first вёрстка
- ❌ Светлая тема как опция
- ❌ 8 экранов wizard

## Референсы
Claude.ai · ChatGPT · Perplexity (НЕ Linear/Cursor/Hex)

## Контекст
- CLAUDE.md — общая концепция
- PROJECT.md — vision, personas, success criteria
- REQUIREMENTS.md — 22 v1 requirements
- design-constraints.md — все запреты verbatim
- screenshots/ — текущий look (3 экрана)

## Задача
[Опиши конкретное изменение]
'@ | Out-File -FilePath (Join-Path $bundleDir 'PROMPT.md') -Encoding UTF8

Write-Output "Bundle создан: $bundleDir"
Write-Output "Открой claude.ai/design, перетащи папку, скопируй PROMPT.md"
```

### T-08-01-5: .claude/rules/ файлы (15 мин)

NEW `.claude/rules/design-bans.md`:
```markdown
# Design Bans (verbatim из CLAUDE.md)

## Из-за провальных итераций v0/v0b
- ❌ «Work-modes» Discovery/Triage/Investigate/Mapping/Knowledge
- ❌ Object-centric IDE с tree метаданных слева
- ❌ Workflow editor с карточками MCP-операций
- ❌ AI right-rail с «инсайтами» / «магическими» подсказками

## Из общей дизайн-философии
- ❌ Inter font, purple-cyan gradients, glass morphism, decorative emoji
- ❌ Mobile-first вёрстка (desktop only, ≥ 1280px)
- ❌ Светлая тема как опция

## Из обратной связи 2026-05-13/17
- ❌ 8 экранов wizard в концепциях
- ❌ Накидывание 6+ LLM провайдеров в UI верхнего уровня
- ❌ PII shield / RAG / Memory tool как обязательные первые экраны
```

NEW `.claude/rules/tech-stack.md`:
```markdown
# Tech Stack (locked)

| Слой | Технология | Версия |
|---|---|---|
| Frontend framework | Next.js (App Router) | 15.5.x |
| UI library | React | 19.x |
| Component lib | shadcn/ui (via @radix-ui) | latest |
| Styling | Tailwind | 4.x |
| Fonts | IBM Plex Sans / Mono | latest |
| Backend | FastAPI | 0.115+ |
| Validation | Pydantic v2 | 2.9+ |
| ASGI | uvicorn | 0.30+ |
| Storage | SQLite via aiosqlite | latest |
| HTTP client | httpx | 0.27+ |
| LLM transport | OpenAI-compatible HTTP | — |
| MCP transport | HTTP Streamable | spec 2024-11-05 |
| Testing — backend | pytest + pytest-asyncio | latest |
| Testing — frontend unit | Vitest | latest |
| Testing — E2E | Playwright | latest |
| Package manager | pnpm | 9.x (fallback: npx через node 22) |
| Desktop wrapper | Electron + electron-builder | 33+ / 26+ |
| Backend bundling | PyInstaller | 6+ |
| Python | CPython | 3.12 |
| Node | Node | 22.14 |
```

NEW `.claude/rules/session-contract.md`:
```markdown
# Session Contract

## Брутальная честность в обе стороны
- Я (Claude) брутально честен с пользователем
- Пользователь брутально честен со мной
- Проверять утверждения перед согласием — не молча пивотить

## MCP — это всё
Никаких абстракций «work-modes», «Object-IDE» сверху MCP. NL → LLM → tool_calls → результат.

## Workflow «Продолжай»
1. Read `.planning/STATE.md` — текущая milestone/phase/progress
2. Read последний active `.planning/phases/<N>/PHASE-summary.md` (если есть)
3. Read `<N>-CONTEXT.md` если active phase в работе
4. Озвучить в ОДНОМ сообщении: на чём остановились + 2-3 опции
5. Ждать команды

## Запрет на wrong project
**Работаем ТОЛЬКО в `C:\CLOUDE_PR\projects\analyst-workspace-design\`**.
НЕ ЛЕЗТЬ в:
- `analyst-tools-1c` (заброшенный v0 Object-IDE концепт)
- Другие папки `C:\CLOUDE_PR\projects\*` (другие проекты)

## Правило 3 итераций
Если за 3 раунда нет прогресса по задаче — STOP, передаём пользователю, переписываем руками.

## Streaming видим
SSE статусы показываем live: «Анализирую → Вызываю tool → Формирую ответ».
```

## Acceptance criteria

1. `.claude/skills/` содержит 3 папки + SKILL.md в каждой
2. `.claude/rules/` содержит 3 .md файла
3. `pwsh .claude/skills/awd-dev-up/scripts/up.ps1` — сервера поднимаются, exit 0 за < 60 сек
4. `pwsh .claude/skills/awd-quality-gate/scripts/gate.ps1` — выводит 4 строки + overall, exit 0 если всё green

## Risks

- **PowerShell execution policy** на чистой машине может блокировать .ps1 → SKILL.md содержит `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned` инструкцию
- **npx через .cmd на Windows** — Process.Start требует .cmd extension explicitly → up.ps1 находит через `Get-Command npx.cmd`
- **Playwright первый запуск** — `npx playwright install chromium` качает ~200 MB → bundle.ps1 ставит молча

## Files

NEW:
- `.claude/skills/awd-dev-up/SKILL.md`
- `.claude/skills/awd-dev-up/scripts/up.ps1`
- `.claude/skills/awd-quality-gate/SKILL.md`
- `.claude/skills/awd-quality-gate/scripts/gate.ps1`
- `.claude/skills/awd-claude-design-handoff/SKILL.md`
- `.claude/skills/awd-claude-design-handoff/scripts/bundle.ps1`
- `.claude/rules/design-bans.md`
- `.claude/rules/tech-stack.md`
- `.claude/rules/session-contract.md`

## Dependencies

Зависит от: Phase 7 ✓ (Electron skeleton — для context)
Блокирует: Plan 8.2 (нужен .claude/skills/ + rules/ для routing)
