---
phase: 04-demo-refine
plan: "04"
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - docs/DEMO-SCRIPT.md
  - docs/DEMO-OBSERVER-CHECKLIST.md
  - docs/DEMO-FEEDBACK-TEMPLATE.md
  - scripts/seed-demo-data.py
  - scripts/__tests__/test_seed_demo_data.py
  - .planning/BACKLOG-POST-MVP.md
  - README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Аналитик может прогнать 15-минутный demo по шагам из docs/DEMO-SCRIPT.md без импровизации"
    - "Наблюдатель отмечает время ответа / UX-затыки / неожиданные вопросы по docs/DEMO-OBSERVER-CHECKLIST.md"
    - "Аналитик заполняет docs/DEMO-FEEDBACK-TEMPLATE.md за ≤10 минут после демо"
    - "scripts/seed-demo-data.py наполняет SQLite ≥5 сессий с примерами 6 типов cards (для preview/dry-run без реальной 1С)"
    - "BACKLOG-POST-MVP.md существует, имеет шаблон для пунктов feedback"
  artifacts:
    - path: "docs/DEMO-SCRIPT.md"
      provides: "Структурированный 15-минутный сценарий: подключение → первый prompt → cards → channel switch → trace → wrap-up"
      min_lines: 150
    - path: "docs/DEMO-OBSERVER-CHECKLIST.md"
      provides: "Чеклист наблюдателя: время ответа, UX-затыки, неожиданные паттерны"
      min_lines: 60
    - path: "docs/DEMO-FEEDBACK-TEMPLATE.md"
      provides: "Шаблон для аналитика: что понравилось/мешало/нужно/3 priority items"
      min_lines: 60
    - path: "scripts/seed-demo-data.py"
      provides: "Заполняет SQLite демо-сессиями с 6 типами cards"
      min_lines: 80
    - path: ".planning/BACKLOG-POST-MVP.md"
      provides: "Шаблон для post-MVP backlog с категориями (UX/Performance/Cards/Productivity/Security)"
      min_lines: 30
  key_links:
    - from: "docs/DEMO-SCRIPT.md"
      to: "docs/USER.md"
      via: "ссылка для подготовки"
      pattern: "USER.md"
    - from: "scripts/seed-demo-data.py"
      to: "backend/app/storage/db.py"
      via: "использует apply_migrations + INSERT для sessions/messages/card_states"
      pattern: "apply_migrations"
    - from: "README.md"
      to: "docs/DEMO-SCRIPT.md"
      via: "секция «Демо для аналитика»"
      pattern: "DEMO-SCRIPT"
---

<objective>
Артефакты для проведения 15-минутного live demo с реальным аналитиком:

1. **DEMO-SCRIPT.md** — пошаговый сценарий с 6 разделами:
   - (2 мин) Подключение MCP + LLM
   - (3 мин) Первый prompt «Расскажи про базу»
   - (4 мин) Сложный prompt с TableCard + sort/CSV
   - (2 мин) Channel switching на вторую базу
   - (2 мин) Trace + Copy as curl
   - (2 мин) Wrap-up + feedback
2. **DEMO-OBSERVER-CHECKLIST.md** — чек-лист наблюдателя
3. **DEMO-FEEDBACK-TEMPLATE.md** — структурированная форма для аналитика
4. **scripts/seed-demo-data.py** — наполнение SQLite демо-данными для preview (без необходимости в реальной 1С). Создаёт ≥5 сессий с messages показывающими все 6 типов cards + anon токены + slash command examples + Cmd-K результаты.
5. **BACKLOG-POST-MVP.md** — структура для записи feedback items по категориям.
6. README.md — добавить секцию «Демо для аналитика» с ссылкой на DEMO-SCRIPT.

Это **процессный план** — НЕТ нового кода продукта. Только документы и один Python script для seeding.

Сам прогон демо — human activity, выполняется после merge Phase 4.

Output: 5 markdown файлов + 1 Python script (~80 строк) + 1 секция в README.
</objective>

<execution_context>
@C:/CLOUDE_PR/.claude/get-shit-done/workflows/execute-plan.md
@C:/CLOUDE_PR/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-demo-refine/04-CONTEXT.md
@.planning/phases/04-demo-refine/04-01-PLAN.md
@.planning/phases/04-demo-refine/04-02-PLAN.md
@.planning/phases/04-demo-refine/04-03-PLAN.md
@.planning/phases/03-production-ready/03-04-SUMMARY.md
@docs/USER.md
@docs/API.md
@docs/CURL.md
@README.md
@backend/app/storage/migrations.py
@backend/app/orchestrator/persistence.py
@backend/app/orchestrator/cards.py
</context>

<tasks>

<task type="auto">
  <name>T-04-04-1: docs/DEMO-SCRIPT.md + DEMO-OBSERVER-CHECKLIST.md + DEMO-FEEDBACK-TEMPLATE.md</name>
  <files>docs/DEMO-SCRIPT.md, docs/DEMO-OBSERVER-CHECKLIST.md, docs/DEMO-FEEDBACK-TEMPLATE.md</files>
  <action>
1. `docs/DEMO-SCRIPT.md` (~180 строк):

   Структура:
   - **Перед демо** (~10 мин до встречи): проверить что запущен docker compose, в Settings уже добавлены 2 MCP + LLM, сессии пустые (или seed-demo-data.py запущен — выбор fasilator'а). Проверить что Cmd+K работает, прокинуть URL аналитику.
   - **Цель демо**: получить честный фидбек от аналитика на 15-минутном прогоне ключевых сценариев. НЕ продать tool, НЕ показать каждую фичу.

   **Разделы**:

   **0. Введение (1 мин)** — Что это: чат с MCP-toolkit. Аналитик пишет на NL, LLM сама решает что вызывать. Цель встречи — фидбек.

   **1. Подключение (2 мин)**
   - Settings → MCP Connections → видны 2 подключённые базы (заранее настроенные)
   - Click Ping каждой → ожидаемо green
   - Settings → LLM → API key persisted; click Test → ✓ completion работает
   - Возврат на главную → Empty state если первый раз; либо sessions sidebar

   **2. Первый prompt — простой обзор (3 мин)**
   - Активный channel: «РТ» (или УСО)
   - Quick prompt chip click: «Обзор базы — перечисли подсистемы, основные документы»
   - Ожидаемо: ≤30 сек, ответ с TL;DR + ObjectCard или TableCard
   - Visible: streaming status «Анализирую → Вызываю get_metadata → Формирую ответ»
   - Trace панель: click → 1 tool call, args, duration

   **3. Сложный prompt — журнал + cards (4 мин)**
   - Type slash: `/journal Период=Час, Уровень=Error+Warning` → expand prompt → отправить
   - Ожидаемо: LogCard с записями, color-coded
   - Click «Загрузить ещё» → cursor-fetch entries
   - Type другой prompt: «Покажи документы реализации за вчера, group by контрагент»
   - Ожидаемо: TableCard, sort колонок, click «Экспорт CSV» → file скачан
   - Если LLM возвращает агрегаты (e.g. «Сумма за месяц») — может быть MetricCard

   **4. Anonymization (2 мин)**
   - Header click Anonymization → ON (амбар)
   - Type prompt: «Найди контрагентов с задолженностью более 100к» → ответ содержит токены `[ORG-001]`, `[INN-001]`
   - В TableCard footer: «Раскрыть реальные значения» button
   - Click → реальные имена inline, badge «Реальные значения»

   **5. Channel switching (2 мин)**
   - Header dropdown → переключить на вторую базу
   - Sidebar: новые сессии
   - Type prompt: «Найди где используется Контрагент» (slash `/find Контрагент`)
   - Ожидаемо: ReferencesCard со списком grouped by usage_kind
   - Click элемент → отправляется новый prompt `Покажи {name}`

   **6. Trace + curl + Cmd-K (1 мин)**
   - Раскрыть trace последнего ответа
   - Click «Скопировать как curl» → toast «Скопировано»
   - Cmd+K → CommandPalette → typing «реализаци» → результаты со snippet
   - Click result → переход на /sessions/{id}, scroll к message

   **7. Wrap-up (1 мин)**
   - Спасибо, передать FEEDBACK-TEMPLATE
   - Согласовать дедлайн для заполнения (24h)

   **После демо**: extract feedback в BACKLOG-POST-MVP.md по категориям.

   **Резервные сценарии** (если что-то ломается):
   - MCP не отвечает → показать red banner + retry
   - LLM rate limit → toast countdown
   - Confirm dialog: type «/sql DROP TABLE X» → Dialog появляется → юзер видит safety net

2. `docs/DEMO-OBSERVER-CHECKLIST.md` (~80 строк):

   Структура:
   - **Цель**: фиксировать behavioral data без анализа во время демо.
   - **До демо**: открыть документ, подготовить таблицу.
   - **Во время демо** — отметить time-stamp + наблюдение по категориям:

   **Категория 1: Время ответа**
   - [ ] Первый chunk SSE: ≤500мс? фактическое значение __
   - [ ] Полный ответ простого prompt: ≤10с? фактическое __
   - [ ] Полный ответ сложного prompt (2-3 tool calls): ≤30с? фактическое __

   **Категория 2: UX-затыки**
   - [ ] Анализ ищет что-то и не находит → где? как долго ищет?
   - [ ] Кнопка не очевидна → какая?
   - [ ] Пользователь спрашивает «а как X?» → какой X?
   - [ ] Ошибочно использовал фичу не по назначению → какая?
   - [ ] Stuck на ≥10с без действия → когда?

   **Категория 3: Неожиданные паттерны**
   - [ ] Спросил то, что мы не ожидали → запрос:
   - [ ] Использовал фичу X способом не задуманным → как?
   - [ ] Игнорировал фичу Y → какая?

   **Категория 4: Технические сбои**
   - [ ] SSE завис → когда / при каком prompt
   - [ ] MCP отвалился → причина:
   - [ ] LLM выдал ошибку → код ошибки:
   - [ ] Карточка не отрисовалась → tool name + raw output

   **Категория 5: Эмоциональные сигналы**
   - [ ] Положительная реакция («о, классно!») → на что
   - [ ] Раздражение → на что
   - [ ] Замешательство → на что
   - [ ] Скепсис → на что

   **После демо** — передать заметки в BACKLOG-POST-MVP.md.

3. `docs/DEMO-FEEDBACK-TEMPLATE.md` (~80 строк):

   Структура (markdown form для копирования):

   ```markdown
   # Demo Feedback — {Аналитик ФИО}, {Дата}

   ## Контекст
   - Длительность: __ мин
   - Используемые базы: __
   - Текущие инструменты аналитика (чем пользуешься сейчас): __

   ## 1. Что понравилось (5 минут на ответ)
   _3-5 пунктов конкретно_
   - ___
   - ___

   ## 2. Что мешало
   _3-5 пунктов, по возможности с примерами prompts_
   - ___

   ## 3. Чего не хватает (топ-3)
   _Только 3 — не больше_
   1. ___
   2. ___
   3. ___

   ## 4. Частота использования (если внедрим)
   - [ ] Каждый день
   - [ ] Несколько раз в неделю
   - [ ] При сложных задачах (раз в неделю)
   - [ ] Эпизодически (раз в месяц)
   - [ ] Не буду пользоваться, потому что: __

   ## 5. Priority items для следующего релиза
   _3 пункта, отсортированные по важности_
   1. _Самое важное_: ___
   2. _Важно_: ___
   3. _Желательно_: ___

   ## 6. Любые открытые комментарии
   ___
   ```

Karpathy: structured templates, нет «открытых полей». Конкретно: ровно 3 priority items, ровно 3-5 пунктов в первых секциях, чтобы аналитик не разбегался.

Brutal honesty: documenting в SUMMARY — это процессные артефакты, ценность доказывается только после реального демо. Если demo не проведено в течение 14 дней после merge — артефакты «протухают» (имена tools / cards могут измениться).
  </action>
  <verify>
    <automated>test -f docs/DEMO-SCRIPT.md && test -f docs/DEMO-OBSERVER-CHECKLIST.md && test -f docs/DEMO-FEEDBACK-TEMPLATE.md && wc -l docs/DEMO-SCRIPT.md docs/DEMO-OBSERVER-CHECKLIST.md docs/DEMO-FEEDBACK-TEMPLATE.md && grep -L "TODO\|FIXME\|placeholder" docs/DEMO-SCRIPT.md docs/DEMO-OBSERVER-CHECKLIST.md docs/DEMO-FEEDBACK-TEMPLATE.md</automated>
  </verify>
  <done>
- 3 файла существуют
- DEMO-SCRIPT.md ≥ 150 строк, 7 разделов (0-7), 15 минут timing visible
- DEMO-OBSERVER-CHECKLIST.md ≥ 60 строк, 5 категорий
- DEMO-FEEDBACK-TEMPLATE.md ≥ 60 строк, 6 секций, structured choices
- 0 TODO/FIXME/placeholder в этих файлах
- Все ссылки на USER.md, API.md, BACKLOG-POST-MVP.md проверены (paths существуют)
  </done>
</task>

<task type="auto">
  <name>T-04-04-2: scripts/seed-demo-data.py + BACKLOG-POST-MVP.md + README update</name>
  <files>scripts/seed-demo-data.py, scripts/__tests__/test_seed_demo_data.py, .planning/BACKLOG-POST-MVP.md, README.md</files>
  <action>
1. `scripts/seed-demo-data.py` (~120 строк):
   - CLI: `python scripts/seed-demo-data.py [--db backend/data.db] [--clean]`
   - Если `--clean` — wipe sessions/messages/card_states (NOT mcp_connections, NOT llm_settings)
   - apply_migrations on connect (idempotent — должен дойти до v5 от 04-03 если применён)
   - Создать 5 сессий с разными channel_id (демо: «РТ» и «УСО»):
     - Sess 1 «Обзор базы РТ»: 1 user msg + 1 assistant с TableCard (5 documents)
     - Sess 2 «Журнал за час»: 1 user + 1 assistant с LogCard (10 entries: Error/Warning/Info mix)
     - Sess 3 «Где используется Контрагент»: 1 user + 1 assistant с ReferencesCard (4 groups: Реквизит/Подчинённый/Шаблон/Право)
     - Sess 4 «Метрика прихода за месяц»: 1 user + 1 assistant с MetricCard sparkline (12 points)
     - Sess 5 «Покажи Процедуру X»: 1 user + 1 assistant с CodeCard (BSL example)
   - Дополнительно: 1 сессия в режиме anonymization (содержит токены `[ORG-001]`, `[INN-001]` + card_states.anon_tokens заполнен)
   - Auto-titles установлены сразу (не дёргаем LLM)
   - tool_calls в каждом assistant — фиктивные с реалистичными args (для Trace demo)
   - Использует app.storage.db, app.orchestrator.persistence функции — НЕ raw SQL (consistent)

   Структура:
   ```python
   import asyncio
   import argparse
   from datetime import datetime, timedelta
   import json
   import sys
   from pathlib import Path

   sys.path.insert(0, str(Path(__file__).parent.parent / "backend"))

   from app.storage.db import get_db_path, init_db
   from app.storage.migrations import apply_migrations
   import aiosqlite

   DEMO_SESSIONS = [...]  # data dicts

   async def seed(db_path: str, clean: bool):
       async with aiosqlite.connect(db_path) as db:
           await apply_migrations(db)
           if clean:
               await db.execute("DELETE FROM messages")
               await db.execute("DELETE FROM card_states")
               await db.execute("DELETE FROM sessions")
               await db.commit()
           for sess in DEMO_SESSIONS:
               # INSERT session + messages + cards
               ...
           await db.commit()
       print(f"Seeded {len(DEMO_SESSIONS)} sessions")

   if __name__ == "__main__":
       parser = argparse.ArgumentParser()
       parser.add_argument("--db", default="backend/data.db")
       parser.add_argument("--clean", action="store_true")
       args = parser.parse_args()
       asyncio.run(seed(args.db, args.clean))
   ```

   Brutal: hardcoded data — для demo достаточно. Без mock LLM, без real MCP — только статичные JSON payloads которые соответствуют card schemas из cards.py.

2. `scripts/__tests__/test_seed_demo_data.py` (~50 строк):
   - test_seed_creates_5_sessions
   - test_seed_creates_messages_for_each_session
   - test_seed_creates_log_card_state_when_card_id_present
   - test_seed_clean_flag_wipes_previous_data
   - test_seed_idempotent_without_clean_skips_existing (или просто проверка что повторный запуск НЕ дублирует sessions — INSERT OR IGNORE)
   - test_seed_anon_session_has_anon_tokens_in_card_state
   (≥ 6 тестов)
   Pytest fixture с tmp_path / временной БД.

3. `.planning/BACKLOG-POST-MVP.md` (~50 строк):

   ```markdown
   # Backlog: Post-MVP

   **Создан**: {дата завершения Phase 4}
   **Источники**: feedback из demo сессий + deferred items из roadmap.

   ## Категории

   ### UX
   - [ ] (P0/P1/P2) ___
   ### Performance
   - [ ] ___
   ### Cards (расширенные типы / drill-down)
   - [ ] ___
   ### Productivity (slash/mention/cmd-k extensions)
   - [ ] ___
   ### Security
   - [ ] ___
   ### Tech debt
   - [ ] PROD-05 PDF export — partial: CSV есть; PDF требует pdfkit / server-side render
   - [ ] Vector search / RAG — отложено за пределы MVP
   - [ ] Cross-card раскрытие анонимизированных токенов (Plan 4.1 SUMMARY)

   ## Priority Legend
   - **P0**: блокирует пилотное использование
   - **P1**: ухудшает UX, делает реже использовать
   - **P2**: nice-to-have

   ## Pipeline
   1. Add — добавить пункт сюда после demo feedback session
   2. Triage — раз в неделю просмотр + priority assignment
   3. Plan — пункты P0/P1 идут в следующий phase (Phase 5)
   ```

4. `README.md` — добавить новую секцию `## Демо для аналитика`:
   ```markdown
   ## Демо для аналитика

   Перед прогоном:
   1. Запустить `docker compose up`
   2. (Опционально) Заполнить демо-данными: `python scripts/seed-demo-data.py --clean`
   3. Открыть `docs/DEMO-SCRIPT.md` — 15-минутный сценарий

   Во время демо:
   - Наблюдатель ведёт `docs/DEMO-OBSERVER-CHECKLIST.md`
   - После — аналитик заполняет `docs/DEMO-FEEDBACK-TEMPLATE.md`

   После демо — feedback → `.planning/BACKLOG-POST-MVP.md`
   ```

Karpathy: один Python script ~120 строк, hardcoded data. Test fixture использует tmp_path — изолированная БД. README секция короткая, ссылки.

Brutal honesty: seed-demo-data.py создаёт «искусственные» данные без LLM/MCP — это значит при demo run-through на seed'е аналитик увидит cards как «mock'и». Для **реального demo** на живой 1С seed не нужен. Документировать это в SUMMARY и DEMO-SCRIPT.md «Опционально для preview без 1С».
  </action>
  <verify>
    <automated>cd backend && python -m pytest ../scripts/__tests__/test_seed_demo_data.py -v && cd .. && python scripts/seed-demo-data.py --db /tmp/test-seed.db --clean && sqlite3 /tmp/test-seed.db "SELECT COUNT(*) FROM sessions" && grep -L "TODO\|FIXME" scripts/seed-demo-data.py .planning/BACKLOG-POST-MVP.md README.md</automated>
  </verify>
  <done>
- ≥ 6 тестов test_seed_demo_data.py зелёные
- Запуск `python scripts/seed-demo-data.py --db /tmp/test-seed.db --clean` не падает, создаёт ≥5 sessions
- BACKLOG-POST-MVP.md существует, имеет 6 категорий + 3 предзаполненных tech-debt items + Pipeline section
- README.md содержит секцию «Демо для аналитика» с ссылками на DEMO-SCRIPT/CHECKLIST/FEEDBACK/BACKLOG
- 0 TODO/FIXME в новых файлах
- Все ссылки внутри docs/*.md проверены — paths существуют
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed-demo-data.py → backend/data.db | trusted operator script, локально |
| docs/* → public repo | public (если repo OSS); никаких секретов |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-21 | Information Disclosure | seed-demo-data может содержать sensitive examples | accept | используем выдуманные данные «ООО Ромашка», ИНН 0000000000 |
| T-04-22 | Tampering | seed --clean wipes user data | accept | requires explicit --clean flag; backup via git is operator responsibility |
| T-04-23 | Privacy | feedback template может содержать имя пользователя | accept | template — markdown файл, заполняется юзером, хранится локально или в private repo |
</threat_model>

<out_of_scope>
- Автоматизация прогона demo (Playwright тест) — задумано как human session
- Видео-запись demo (выбор фасилитатора)
- Анализ feedback через NLP / sentiment scoring — manually для MVP
- Шаблоны demo на других языках — только русский
- Multi-session demo aggregation — manually собираем в backlog
- Реальное seed-demo-data из живой 1С (требует подключения к real MCP — out of scope для preview script)
- Auto-rotation seed data при каждом старте — only on demand
- A/B testing разных Demo scripts
</out_of_scope>

<risks>
1. **Demo артефакты протухнут** — если код продукта меняется после Phase 4, имена tools/cards могут отличаться от DEMO-SCRIPT. Mitigation: SUMMARY указывает дату валидности; ревизия script перед каждым demo.

2. **seed-demo-data использует internal API (persistence functions)** — если API меняется, script ломается. Mitigation: тесты test_seed_demo_data.py ловят регрессии.

3. **seed-demo-data --clean опасен** — wipe production data. Mitigation: явный flag, default off, README предупреждение.

4. **Demo может пойти не по plan** — реальный аналитик может задать «крестики-нолики» вместо «обзор базы». Резервные сценарии в DEMO-SCRIPT.

5. **Feedback template слишком жёсткий** (только 3 priority items) — может frustrating если аналитик хочет 5. Mitigation: «3 — это минимум должен сказать, если есть больше — секция «Открытые комментарии»».

6. **BACKLOG-POST-MVP.md растёт неконтролируемо без regular triage** — Pipeline section фиксирует «раз в неделю». Этот ритуал не обеспечивается планом — это организационная задача.
</risks>

<verification>
End-to-end smoke (manual после execute):
1. `docker compose up` → не сломалось от seed script
2. `python scripts/seed-demo-data.py --db /tmp/preview.db --clean` → создаёт 5+ sessions
3. Запуск backend с этой БД → frontend показывает sessions, cards, anon токены, ReferencesCard и т.д.
4. Read-through DEMO-SCRIPT.md — каждая ссылка работает (на USER.md, API.md, etc)
5. Read-through DEMO-OBSERVER-CHECKLIST.md — все 5 категорий понятны
6. Read-through DEMO-FEEDBACK-TEMPLATE.md — заполнение занимает ≤10 мин (self-test)

Автоматическое:
```
cd backend && python -m pytest ../scripts/__tests__/test_seed_demo_data.py -v
test -f docs/DEMO-SCRIPT.md && test -f docs/DEMO-OBSERVER-CHECKLIST.md && test -f docs/DEMO-FEEDBACK-TEMPLATE.md && test -f .planning/BACKLOG-POST-MVP.md && test -f scripts/seed-demo-data.py
wc -l docs/DEMO-SCRIPT.md docs/DEMO-OBSERVER-CHECKLIST.md docs/DEMO-FEEDBACK-TEMPLATE.md .planning/BACKLOG-POST-MVP.md scripts/seed-demo-data.py
grep -rn "TODO\|FIXME\|placeholder" docs/DEMO-SCRIPT.md docs/DEMO-OBSERVER-CHECKLIST.md docs/DEMO-FEEDBACK-TEMPLATE.md .planning/BACKLOG-POST-MVP.md scripts/seed-demo-data.py | grep -v '^#' | wc -l   # 0
grep "DEMO-SCRIPT" README.md   # 1 hit (ссылка)
python scripts/seed-demo-data.py --db /tmp/test-seed.db --clean && sqlite3 /tmp/test-seed.db "SELECT COUNT(*) FROM sessions"   # ≥5
```
</verification>

<test_strategy>
- pytest для seed-demo-data.py (создание, idempotency, clean flag, anon_tokens)
- НЕ юнит-тесты для markdown файлов (только structural verify через wc -l и grep)
- НЕ E2E (Playwright) — demo это human session
</test_strategy>

<references>
- 04-CONTEXT.md `<decisions>` Plan 4.4 + Demo script structure
- 04-01-PLAN.md (anon токены в demo session)
- 04-02-PLAN.md (6 типов cards — seed data покрывает все 6)
- 04-03-PLAN.md (slash/Cmd-K показываются в demo)
- ROADMAP.md Phase 4 (15-минутное demo — success criterion #1)
- 03-04-SUMMARY.md (документация pattern USER.md, API.md)
</references>

<success_criteria>
1. 3 markdown демо-артефакта существуют с минимальными размерами и без TODO
2. seed-demo-data.py запускается без ошибок, создаёт ≥5 sessions с 6 типами cards
3. BACKLOG-POST-MVP.md инициализирован с 6 категориями + Pipeline ритуал
4. README.md содержит секцию «Демо для аналитика» с правильными ссылками
5. ≥ 6 тестов seed скрипта зелёные
6. Все 6 типов cards (table/object/log/metric/references/code) представлены в seed data
7. Хотя бы 1 seed session демонстрирует anon токены [ORG-001]
8. 0 TODO/FIXME/placeholder
9. Demo может быть прогнан без импровизации (DEMO-SCRIPT.md содержит конкретные prompts/clicks для каждого шага)
</success_criteria>

<output>
`.planning/phases/04-demo-refine/04-04-SUMMARY.md` с frontmatter (phase, plan, subsystem=demo-artifacts, tags) + что сделано + метрики + явная заметка что реальный demo с аналитиком — human activity после merge, документы протухают если задержка >14 дней.
</output>
