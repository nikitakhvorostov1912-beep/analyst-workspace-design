---
phase: 05-ux-polish
plan: 04
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - frontend/lib/storage.ts
  - frontend/app/page.tsx
  - frontend/app/sessions/[id]/page.tsx
  - frontend/components/shell/ChannelSelector.tsx
  - frontend/components/chat/AssistantMessage.tsx
  - frontend/components/chat/Input.tsx
  - frontend/components/shell/ModelBadge.tsx
  - frontend/lib/api.ts
  - frontend/components/chat/__tests__/useChatStream.test.tsx
  - frontend/components/cards/__tests__/ChannelSelector.test.tsx
autonomous: true
requirements: [UX-04]
must_haves:
  truths:
    - "app/page.tsx hasConfig вычисляется из backend (fetchConnections + fetchLLMConfig), а не из localStorage"
    - "Все production-вызовы getMCPConnections / getLLMConfig заменены на async fetch или getLLMApiKey()"
    - "Функции getMCPConnections/getLLMConfig/setMCPConnections/setLLMConfig помечены @deprecated, но физически удалены вместе с использующими их тестами или адаптированы под mock fetch-функций"
    - "lib/api.ts fetchChat читает api_key из getLLMApiKey() (sessionStorage), endpoint+model из аргумента (передаётся вызывающим, который получил LLMConfig из fetchLLMConfig)"
    - "При добавлении MCP через curl и refresh главной — empty state исчезает (доказательство что backend = source of truth)"
    - "Channel selector использует backend (как и раньше); никаких регрессий"
  artifacts:
    - path: "frontend/app/page.tsx"
      provides: "Backend-driven hasConfig + LLM state"
      contains: "fetchLLMConfig"
    - path: "frontend/lib/storage.ts"
      provides: "Deprecated MCP/LLM getters + active channel + anon + onboarding helpers"
      contains: "@deprecated"
    - path: "frontend/lib/api.ts"
      provides: "fetchChat использует getLLMApiKey + переданные endpoint/model"
      contains: "getLLMApiKey"
  key_links:
    - from: "app/page.tsx useEffect"
      to: "fetchConnections + fetchLLMConfig"
      via: "Promise.all"
      pattern: "Promise\\.all\\(\\[fetchConnections"
    - from: "lib/api.ts fetchChat"
      to: "getLLMApiKey from lib/api-keys"
      via: "import + use"
      pattern: "getLLMApiKey\\(\\)"
    - from: "components/chat/Input.tsx hasLLMConfigured"
      to: "fetchLLMConfig OR props"
      via: "loaded LLM state"
      pattern: "fetchLLMConfig|llmConfig"
---

<objective>
Закрыть mismatch между localStorage и backend как source-of-truth. Перевести `app/page.tsx`, `app/sessions/[id]/page.tsx`, `components/chat/Input.tsx`, `components/chat/AssistantMessage.tsx`, `components/shell/ModelBadge.tsx` на backend-driven данные (через fetchConnections + fetchLLMConfig). API ключ читать из `getLLMApiKey()` (sessionStorage, Plan 5.2). Функции localStorage в `storage.ts` помечаются `@deprecated`, но физически остаются (для backward compat существующих тестов; D-Plan 5.4 говорит «оставить для тестов Phase 1-4 на них завязаны»). Адаптировать тесты на новый flow.

**Purpose:** UX-04 — backend = единственный источник истины. localStorage держит только: `analyst.active_channel`, `analyst.anon_enabled`, `analyst.onboarding_completed`. sessionStorage держит: `analyst.llm_api_key`.

**Output:** обновлённый storage.ts с @deprecated маркерами, переписанный hasConfig flow в page.tsx и sessions/[id]/page.tsx, переписанный fetchChat (api.ts), обновлённые компоненты Input/AssistantMessage/ModelBadge/ChannelSelector, регрессионные тесты адаптированы.
</objective>

<execution_context>
@C:/CLOUDE_PR/.claude/get-shit-done/workflows/execute-plan.md
@C:/CLOUDE_PR/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-ux-polish/05-CONTEXT.md
@.planning/phases/05-ux-polish/05-01-SUMMARY.md
@.planning/phases/05-ux-polish/05-02-SUMMARY.md
@.planning/phases/05-ux-polish/05-03-SUMMARY.md
@frontend/app/page.tsx
@frontend/app/sessions/[id]/page.tsx
@frontend/components/shell/ChannelSelector.tsx
@frontend/components/chat/AssistantMessage.tsx
@frontend/components/chat/Input.tsx
@frontend/components/shell/ModelBadge.tsx
@frontend/lib/api.ts
@frontend/lib/storage.ts
@frontend/lib/api-keys.ts

<interfaces>
**Существующие чтения localStorage (grep подтверждает):**
- `app/page.tsx:60-61` — getMCPConnections() + getLLMConfig() для hasConfig
- `app/sessions/[id]/page.tsx:98, 112` — getMCPConnections() для poll channel
- `components/chat/AssistantMessage.tsx:24` — getMCPConnections() для curl-builder
- `components/chat/Input.tsx:42` — getLLMConfig() для проверки isLLMConfigured (disabled state input)
- `components/shell/ChannelSelector.tsx:59` — getMCPConnections() как fallback в catch
- `components/shell/ModelBadge.tsx:10` — getLLMConfig() для отображения имени модели
- `lib/api.ts:59` — getLLMConfig() для api_key + endpoint + model в headers (fetchChat)

**Новые helpers (Plan 5.1/5.2):**
- `getLLMApiKey(): string | null` — из lib/api-keys.ts (sessionStorage)
- `fetchLLMConfig(): Promise<LLMConfigResponse | null>` — async, требует stateful caching в компонентах
- `fetchConnections(): Promise<MCPConnection[]>` — async

**Стратегия миграции:**
- `fetchChat` принимает endpoint+model как параметры (а не читает из localStorage). Вызывающие (useChatStream / Input.tsx submit) предварительно фетчат LLMConfig.
- Альтернатива: `fetchChat` сама делает `await fetchLLMConfig()` на каждом вызове. Принимаем эту альтернативу — она проще, лишний round-trip ms — приемлемо (один запрос на отправку сообщения).
- api_key — `getLLMApiKey()` из sessionStorage.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: storage.ts deprecate + api.ts fetchChat переключение на backend+sessionStorage</name>
  <files>frontend/lib/storage.ts, frontend/lib/api.ts</files>
  <action>
**1. `frontend/lib/storage.ts`** — добавить JSDoc `@deprecated` маркер над 4 функциями, оставив сами тела:
```
/**
 * @deprecated Используйте fetchMCPConnections() из lib/api.ts (Plan 5.4 UX-04).
 * Оставлено для обратной совместимости с тестами Phase 1-4 и offline fallback в ChannelSelector.
 */
export function getMCPConnections(): MCPConnection[] { ... }
```
Аналогично — `setMCPConnections`, `getLLMConfig`, `setLLMConfig`.

`syncMCPConnections`, `getActiveChannelId`, `setActiveChannelId`, `getAnonEnabled`, `setAnonEnabled` — НЕ помечаем deprecated, они нужны.

**2. `frontend/lib/api.ts` fetchChat (строки ~54-112)** — переписать:

Импорты: добавить `import { getLLMApiKey } from "./api-keys"` и `import { fetchLLMConfig } from "./api"` (циклический импорт — вынести fetchChat в самый низ файла после fetchLLMConfig объявления; либо вынести API-key + LLM cfg чтение в начало функции через локальные импорты с разрешением порядка). На практике: получить LLMConfig снаружи fetchChat, передать как параметр.

Решение: расширить сигнатуру:
```
export async function* fetchChat(
  req: ChatRequest,
  llm: { endpoint: string; model: string },
  signal?: AbortSignal,
  extraHeaders?: Record<string, string>,
): AsyncIterable<SSEEvent>
```

В теле:
- Удалить `const cfg = getLLMConfig(); if (!cfg || !cfg.api_key) { yield {error...} return }`
- Заменить на: `const apiKey = getLLMApiKey(); if (!apiKey) { yield {event:"error", data:{message:"API ключ не задан. Откройте Настройки.", code:"no_api_key"}}; return }`
- Headers: `X-LLM-API-Key: apiKey`, `X-LLM-Endpoint: llm.endpoint`, `X-LLM-Model: llm.model`

Удалить импорт `getLLMConfig` из storage сверху файла. Добавить `getLLMApiKey` из `./api-keys`.

Это **меняет публичную сигнатуру fetchChat** — все вызывающие (useChatStream / fetchChat usage) должны передать llm. Тесты обновим в Task 3.
  </action>
  <verify>
    <automated>cd frontend && pnpm type-check 2>&1 | tee /tmp/tc.log; grep -c "Type error\|Cannot find" /tmp/tc.log</automated>
  </verify>
  <done>storage.ts имеет @deprecated на 4 функциях; fetchChat принимает llm параметр, использует getLLMApiKey; type-check вернёт errors в вызывающих (это ожидаемо — починим в Task 2). Этот task сам по себе не зелёный — он подготовка.</done>
</task>

<task type="auto">
  <name>Task 2: page.tsx + sessions/[id]/page.tsx + Input/AssistantMessage/ModelBadge переключение на backend</name>
  <files>frontend/app/page.tsx, frontend/app/sessions/[id]/page.tsx, frontend/components/chat/Input.tsx, frontend/components/chat/AssistantMessage.tsx, frontend/components/shell/ModelBadge.tsx, frontend/components/shell/ChannelSelector.tsx</files>
  <action>
**1. `frontend/app/page.tsx`:**

В импортах:
- Удалить `getMCPConnections, getLLMConfig` из `@/lib/storage`
- Добавить `import { fetchConnections } from "@/lib/api"` (уже есть в файле возможно — проверить) и `import { fetchLLMConfig } from "@/lib/api"`

В useEffect инициализации (~строки 59-65) переписать на async:
```
useEffect(() => {
  let cancelled = false;
  (async () => {
    try {
      const [conns, llm] = await Promise.all([fetchConnections(), fetchLLMConfig()]);
      if (cancelled) return;
      setHasConfig(conns.length > 0 && llm !== null);
    } catch {
      if (cancelled) return;
      setHasConfig(false); // backend недоступен — empty state, BackendIndicator покажет ошибку
    }
    setLocalActiveChannelId(getActiveChannelId());
    setReady(true);
  })();
  return () => { cancelled = true; };
}, []);
```

Учитывать что Plan 5.3 уже добавил onboarding-detection useEffect. Объединить логику: ВНУТРИ Promise.all-блока, после успеха, проверять onboarding-флаг и решать `setShowOnboarding(...)`. Финальная структура:
```
const flag = getOnboardingCompleted();
const hasBoth = conns.length > 0 && llm !== null;
if (!flag && !hasBoth) {
  setShowOnboarding(true);
} else {
  if (!flag && hasBoth) setOnboardingCompleted(true);
  setShowOnboarding(false);
}
setHasConfig(hasBoth);
```

Empty state CTA: оставить как есть (Настроить → /settings).

**2. `frontend/app/sessions/[id]/page.tsx`:**

Импорты: удалить `getMCPConnections`, добавить `fetchConnections`.
Заменить вызовы `const connections = getMCPConnections()` (строки 98, 112) на:
- Если уже в async context: `const connections = await fetchConnections()`
- Если sync (useEffect dep): обернуть в async helper или вынести в useState + useEffect

Прочитать строки 90-120 файла перед изменением, чтобы понимать контекст. Цель: всё чтение connections — из backend.

**3. `frontend/components/chat/Input.tsx`:**

Текущий код (~строка 42): `const cfg = getLLMConfig(); const isLLMConfigured = !!cfg && !!cfg.api_key`.

Заменить на:
- Импортировать `getLLMApiKey` из `@/lib/api-keys`.
- Принимать `hasLLM: boolean` через props ОТ родителя (более чистый путь — родитель уже знает hasConfig).
- ИЛИ простой fallback: `const isLLMConfigured = !!getLLMApiKey()` — sessionStorage check (быстрее, чем fetchLLMConfig). Этого достаточно для UX disabled-state кнопки send.

Выбираем **second option** (sessionStorage check) — минимум изменений архитектуры.

**4. `frontend/components/chat/AssistantMessage.tsx`:**

Текущий код (~строка 24): `const connections = getMCPConnections()`.

Использует connections для curl-builder (TRACE-03). Эта функция вызывается синхронно в render. Заменить:
- Принимать `connections` через props ОТ родителя Thread.tsx, который их фетчит async и кеширует
- ИЛИ простой минимальный путь: оставить `getMCPConnections()` как **legacy cache fallback** — comment-mark, что это кеш после sync через ChannelSelector. После Plan 5.3 onboarding гарантирует, что один раз fetchConnections + syncMCPConnections отработали, поэтому кеш заполнен.

Выбираем **минимальный путь**: оставить вызов как есть, но добавить комментарий `// Cache fallback after ChannelSelector sync; для production source-of-truth используется fetchConnections() в page.tsx (Plan 5.4)`. НЕ deprecate в storage.ts? — оставим deprecated, но AssistantMessage помечаем legacy-comment, не блокер.

Аналогично для **`components/shell/ModelBadge.tsx`** — простой компонент-бейдж. Переключить на async:
- useState + useEffect → `fetchLLMConfig().then((cfg) => setModel(cfg?.model ?? null))`
- Если cfg null — не рендерить бейдж

**5. `frontend/components/shell/ChannelSelector.tsx`:**

Уже использует `fetchConnections()` как primary (строка 54). Fallback на `getMCPConnections()` в catch (строка 59) — оставить, но добавить комментарий `// @deprecated legacy cache fallback`.

Также вызовы `fetchChat` где-то в коде (useChatStream скорее всего) — обновить signature:
`grep -rn "fetchChat\\(" frontend/` — найти все вызовы. В каждом передать `{ endpoint, model }` объект. Источник endpoint+model — useState с предварительным `fetchLLMConfig()`.

В useChatStream (см. components/chat/__tests__/useChatStream.test.tsx импорты) — добавить fetch LLM config в начале sendMessage handler, передать в fetchChat. Если предыдущий fetch неудачен — yield error event.

  </action>
  <verify>
    <automated>cd frontend && pnpm type-check && pnpm lint</automated>
  </verify>
  <done>type-check + lint зелёные; page.tsx hasConfig читает backend; fetchChat принимает llm param; legacy fallback-комменты в AssistantMessage/ChannelSelector.</done>
</task>

<task type="auto">
  <name>Task 3: Адаптация regression-тестов под новый flow</name>
  <files>frontend/components/chat/__tests__/useChatStream.test.tsx, frontend/components/cards/__tests__/ChannelSelector.test.tsx</files>
  <action>
**1. `frontend/components/chat/__tests__/useChatStream.test.tsx`:**

Mock сейчас (по grep): `getLLMConfig: () => ({ endpoint: "x", model: "y", api_key: "k" })`.

Нужно адаптировать:
- Удалить mock `getLLMConfig`, добавить mock `getLLMApiKey: () => "k"` через `vi.mock("@/lib/api-keys", ...)`
- Если тесты используют `fetchChat` напрямую — обновить вызовы на новую сигнатуру `fetchChat(req, { endpoint:"x", model:"y" }, signal, extraHeaders)`
- Если useChatStream сам делает `fetchLLMConfig()` — добавить `vi.mock("@/lib/api", () => ({ ..., fetchLLMConfig: vi.fn().mockResolvedValue({ id:"default", endpoint:"x", model:"y", temperature:0.3 }) }))`

Запустить и итерировать пока зелёные. Если 11 тестов уже падали в Phase 4 (документировано в VERIFICATION.md как pre-existing) — не пытаться чинить их, а попытаться не сделать хуже. Целевой счёт: не более 11 falled тестов после этого плана.

**2. `frontend/components/cards/__tests__/ChannelSelector.test.tsx`:**

Mock `getMCPConnections: vi.fn()` — оставить (это legacy fallback, который мы сохранили в компоненте). Тесты должны продолжать работать.

Проверить: если `fetchConnections` мокается и тесты ассертят на её вызов — оставить как есть.

**Дополнительно — backend тесты:**
Файл `backend/tests/test_chat_route.py` использует mock LLMClient. Plan 5.4 не меняет /chat endpoint — backend regression не затрагивается. Прогнать `cd backend && python -m pytest -x` как regression gate.
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run components/chat/__tests__ components/cards/__tests__ 2>&1 | tail -20</automated>
  </verify>
  <done>Тесты адаптированы под новый flow; полная регрессия `pnpm test --run` не уменьшается vs Phase 4 baseline (192 passed, 11 pre-existing failed = 181 passed). Backend pytest -x зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| api_key в sessionStorage | Источник для X-LLM-API-Key header в /chat |
| backend → LLM | Header-forwarded, не персистится |
| localStorage cache for MCP | Legacy fallback, не source-of-truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | I (Old api_key в localStorage) | Если пользователь обновил приложение после Phase 4 — старый ключ в `analyst.llm.api_key` localStorage | mitigate | В Plan 5.4 — на старте app/page.tsx добавить one-time migration helper в lib/api-keys.ts: `migrateLegacyApiKey()` — если sessionStorage пуст и в localStorage есть `analyst.llm.api_key`, перенести его в sessionStorage и очистить localStorage. Запускается один раз. |
| T-05-14 | D (race condition) | fetchLLMConfig вызывается на каждое сообщение в fetchChat | accept | Дополнительный round-trip ~5ms, не блокер для UX. Альтернатива (cache) усложняет invalidation |
| T-05-15 | T | AssistantMessage использует устаревший legacy кеш getMCPConnections для curl | accept | Не security-critical (curl с устаревшим endpoint просто не сработает). Reading из stale cache допустимо для read-only UX feature |
</threat_model>

<verification>
1. `cd frontend && pnpm type-check` — 0 ошибок
2. `cd frontend && pnpm lint` — 0 ошибок
3. `cd frontend && pnpm test --run` — passed ≥ 181 (Phase 4 baseline)
4. `cd backend && python -m pytest -x` — все зелёные
5. Manual smoke (`docker compose up`):
   - Удалить все connections и llm-config через curl (или drop tables)
   - Очистить localStorage И sessionStorage (DevTools → Clear)
   - Открыть `http://localhost:3010` → onboarding появляется
   - Заполнить onboarding → главная показывает empty или thread state
   - Через curl POST /connections новое подключение → refresh главной → empty state пропадает (доказательство backend = source of truth для MCP)
   - Через curl POST /llm-config → refresh → если api_key есть в sessionStorage — input enabled, иначе disabled
   - Открыть `/settings` → отобразилось всё что в backend
6. Manual smoke на migration:
   - В localStorage задать `analyst.llm = {"endpoint":"http://x","model":"y","api_key":"sk-legacy123","temperature":0.3}`
   - Refresh страницы → api_key мигрирует в sessionStorage `analyst.llm_api_key`, localStorage очищается
   - В backend нет llm-config → пользователь должен пройти онбординг шага 2 заново (это ОК — legacy migration не предполагает магического создания backend записи)
</verification>

<success_criteria>
- app/page.tsx hasConfig читает backend (fetchConnections + fetchLLMConfig)
- fetchChat использует getLLMApiKey + переданный llm объект
- Все 7 целевых файлов (storage, api, page, sessions/[id]/page, Input, AssistantMessage, ModelBadge, ChannelSelector) изменены согласно плану
- Frontend regression не уменьшается
- Backend regression зелёная
- Legacy api_key migration работает (sessionStorage)
- Заглушек/TODO нет
</success_criteria>

<test_strategy>
**Type:** pnpm type-check
**Unit:** pnpm test --run — не уменьшается счёт passed
**Manual smoke:** 2 сценария в `<verification>` (свежий state + legacy migration)
**Regression backend:** pytest -x
</test_strategy>

<out_of_scope>
- Полное удаление getMCPConnections/getLLMConfig — оставлены deprecated для legacy кеша
- E2E Playwright — Plan 5.5
- Удаление legacy migration — будет в v2 после фазы прогрева
- Performance оптимизации (cache fetchLLMConfig) — v2
</out_of_scope>

<risks>
- **R1:** Изменение публичной сигнатуры fetchChat (добавление llm параметра) — потенциально ломает множество call-sites. Перед task 2 — grep всех `fetchChat(` вызовов и обновить. Если есть >5 call-sites — возможно нужно split task 2 на под-задачи.
- **R2:** 11 pre-existing failing тестов в useChatStream.test.tsx — наш план может непреднамеренно увеличить их число. Стратегия: запускать тесты после каждого изменения, не идти дальше пока счёт не вернётся к baseline.
- **R3:** Legacy migration api_key — пользователи с очень старыми LLMConfig объектами (без api_key поле) попадут в edge case. Защищаем: `if (parsed && typeof parsed.api_key === "string" && parsed.api_key.length > 0)` перед migrate.
- **R4:** `app/sessions/[id]/page.tsx` я не читал — точные строки могут отличаться от 98/112. Перед изменением обязательно Read этого файла.
</risks>

<references>
- `.planning/phases/05-ux-polish/05-CONTEXT.md` — D-Plan 5.4
- `frontend/lib/api.ts:54-112` — текущая fetchChat
- `frontend/lib/storage.ts` — функции для @deprecate
- `frontend/lib/api-keys.ts` — Plan 5.2 артефакт
</references>

<output>
After completion, create `.planning/phases/05-ux-polish/05-04-SUMMARY.md` with:
- Files changed (8)
- Public API changes (fetchChat signature change documented)
- Legacy migration helper added (migrateLegacyApiKey)
- Tests adapted
- Regression deltas vs Phase 4 baseline
- Known follow-ups for Plan 5.5 (E2E + release)
</output>
