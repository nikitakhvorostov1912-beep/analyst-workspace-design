# Baseline метрики на feature/v1.3.0-commerce старт

Дата: 2026-05-22
Base commit: `6f95775` chore(gitignore) + 3 WIP коммита с main
Branch start commit: `feature/v1.3.0-commerce` ↓ от `e1f...` (TBD after planning commit)

## Backend (pytest)

```
681 tests collected
672 passed
9 failed
```

**Coverage: 26.58%** (заявленный target 80% не достигнут).

### Pre-existing failures на старте feature ветки (НЕ моя регрессия)

1. `tests/test_memory.py::test_markdown_store_append_writes_section`
2. `tests/test_memory.py::test_markdown_store_remove_matches_substring`
3. `tests/test_memory.py::test_markdown_store_cap_truncates_oversized`
4. `tests/test_migrations_v5.py::test_migration_v5_backfill_messages_into_fts`
5. `tests/test_orchestrator_loop_confirm.py::test_loop_emits_confirm_required_on_dangerous_execute_code` ⚠️ затрагивает W1.2
6. `tests/test_orchestrator_loop_confirm.py::test_loop_approved_continues` ⚠️ затрагивает W1.2
7. `tests/test_trajectory.py::test_logger_handles_multimodal_content`
8. ~~+2 без названия — повторно запустить~~

**Договор:** новые failures я не добавляю. Pre-existing — отдельным тикетом разбора (не в W1 scope).

### Coverage по критичным модулям (из 26.58% total)

- `orchestrator/loop.py` 11% (379/426 не покрыто) — целевой target W2.6 → 60%
- `orchestrator/persistence.py` 13% → W2.6 → 60%
- `orchestrator/mcp_pool.py` 15% → W2.6 → 70%
- `orchestrator/cards.py` 28% → W2.6 → 60%
- `orchestrator/compressor.py` 22% → W2.6 → 50%
- `orchestrator/attachments.py` 22% — пока без target
- `learning/*` — Sprint 3 модули, частично без тестов

## Frontend (vitest)

```
Test Files  43 passed (43)
Tests      317 passed (317)
Duration   40.39s
```

Coverage не измерялся (нет CI-настройки vitest --coverage в pipeline).

## E2E (Playwright)

5 spec файлов в `frontend/e2e/`:
- ✓ `onboarding.spec.ts` (7 сценариев)
- ✓ `settings-crud.spec.ts` (8 сценариев)
- ✓ `design-v2.spec.ts` (5 visual smoke)
- **SKIP** `setup-and-prompt.spec.ts` — главный happy-path не покрыт ⚠️
- **SKIP** `sessions-history.spec.ts` ⚠️
- **SKIP** `channel-switch.spec.ts` ⚠️

Целевой target W2.5 → 5/5 spec'ов активны.

## Кодовая база

- Backend Python LOC: 12 511
- Frontend TS/TSX LOC: 21 995
- **Total LOC**: 34 506

- `loop.py`: **1157 строк** (God-функция, target W2.1 → ≤ 400)
- `cards.py`: 578 строк
- `persistence.py`: 568 строк
- `models.py`: 514 строк
- `connections.py`: 448 строк
- `compressor.py`: 409 строк
- `skill_store.py`: 393 строк

## TypeScript

- `tsc --noEmit` — не запускался baseline, but UI-REVIEW-2026-05-21 упоминает 1 TS-error в тестах
- strict: true, noUncheckedIndexedAccess: true (из tsconfig.json)

## Git состояние

```
Branch: feature/v1.3.0-commerce
Base:   e91d3e5 docs(planning): commercial v1.3.0 roadmap...
Main:   совпадает (только что ответвились)
```

## Дальше: Wave 1 тикеты

См. `.planning/CHECKLIST-COMMERCE-2026-05-22.md`.
